# Human-edited dependency floor. The reproducible, fully-pinned set lives in
# requirements.lock (what Docker installs) — regenerate it after editing this file.
# Optional extras (browser-use) live in requirements-optional.txt.
fastapi>=0.141.1
uvicorn>=0.52.1
aiohttp>=3.14.3
aiosqlite>=0.22.1
openai>=2.52.0
python-docx>=1.2.0
reportlab>=5.0.0
feedparser>=6.0.14
pyyaml>=6.0.3
# Pinned to match the browsers bundled in the Docker base image
# (mcr.microsoft.com/playwright/python — keep the tag in the Dockerfile in sync).
playwright==1.62.0
playwright-stealth>=2.0.3
beautifulsoup4>=4.15.0
lxml>=6.1.1
# browser-use moved to requirements-optional.txt (feature-flagged, heavy tree;
# backend/browser_use_agent.py imports it lazily).
python-dotenv>=1.2.2
pypdf>=6.14.2
python-multipart>=0.0.32
