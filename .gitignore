# Python
.venv
__pycache__/
*.pyc
*.pyo

# Node
node_modules

# Desktop build (Tauri + PyInstaller)
/build/
/dist/
src-tauri/target/
src-tauri/binaries/
src-tauri/binaries-build/
src-tauri/gen/schemas/

# Docker seeded config dir
/config/

# Extension build output
extension/dist/

# TestFlight upload script — carries App Store Connect key/issuer/team ids.
# Copy ios-standalone/scripts/testflight-upload.sh.example and fill in via env.
ios-standalone/scripts/testflight-upload.sh

# Runtime data (repo-root dirs only — anchored so they don't match nested
# source dirs like ios-standalone/.../JobsmithKit/Data on a case-insensitive FS)
/data/
/resumes/

# Personal config (copy config.example.yaml to config.yaml and fill in your details)
config.yaml
config.yaml.bak
config.yaml.local

# Environment
.env

# Browser-Use runtime directories
.browser-profile/
sessions/
failed_screenshots/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings (may contain local paths)
.claude/

# iOS signing config (set your own DEVELOPMENT_TEAM here — see README-IOS-STANDALONE.md)
ios-standalone/Local.xcconfig

# Beta release kit (listing copy + screenshots) — local-only, never committed
beta-release/

# Local session notes
PUBLIC_RELEASE_PLAN.md
*_PLAN.md
MONETIZATION_PLAN.md
RESUME_STYLES_NOTES.md
FUTURE_IDEAS.md
SESSION_CHANGES.md
AUTO_APPLY_RELIABILITY_REPORT.md
GLM *.md
backend/sync/REDESIGN.md
.aider*
